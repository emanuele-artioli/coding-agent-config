# Antigravity — pending verification

Items that were authored or documented from another platform. Close each only
from an Antigravity session after live verification. Hook commands must be
**absolute paths**.

- [ ] Wire SessionStart reminder in `~/.gemini/config/hooks.json` (absolute paths)
  (entry point: `/home/itec/emanuele/HANDOFF-antigravity.md`)
- [ ] Wire Stop / PreInvocation nudge toward `end-of-session`
- [ ] Wire progressive context nudges if event payloads support them
- [ ] Confirm global skills resolve under `~/.gemini/config/skills/`
- [ ] Confirm `pending-verification` SessionStart reminder is visible
