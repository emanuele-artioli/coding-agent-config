> Historical dispatch, superseded after the Gate A/B audit. Use [overnight recovery](overnight-recovery.md) for new work.

# Overnight Gate A execution prompt

Work autonomously for up to eight hours of wall time on PointStream. Implement and run experiments; do not stop after planning or after the first completed sweep. The priority is a credible competitive regime, followed by as much Gate B/C/D preparation or execution as the evidence and remaining time allow. A win is an objective, never a result to assume. No need to launch a new task or buy services.

Start from fresh origin/main containing the PR #73 review follow-up (baseline before that follow-up: 74953ab). Read AGENTS.md, the host rules, PLAN.md, docs/roadmap.md, docs/workflow/session/SKILL.md, and the codec/evaluation/data areas. Consult docs/history/findings.md before interpreting results. Create your own branch and worktree; preserve other sessions. Record the actual starting SHA and claim your assignment in PLAN.md. Use the existing PointStream environment, configure the external data root, and verify imports resolve to your worktree. Do not mutate the shared environment or use the unsafe cleanup helper.

Own CODEC-ACT-01/02, DATA-ACT-01/04 and EVAL-ACT-01/02/03/04 as needed for this experiment chain. Relevant implementation scope is background/appearance codecs and their config/serialization/client plumbing, metric evaluation, experiments/tier and long-scene manifests, plus focused tests and area updates. Keep generation off initially. Consult the source before trusting old proposed commands. Recover #69–#72 procedures through docs/history/retired-documents.tsv when needed.

Use this approximate schedule, adapting to measured bottlenecks:

1. First 45 minutes: inspect GPU ownership, live jobs, CPU/memory/disk availability and actual encoder/decoder paths/builds. Do not stop other jobs. Verify a tiny end-to-end run, external output paths, complete payload accounting, and calibration controls. Audit source exposure and reserve fresh confirmation candidates before any tuning. The seven previously used videos are development data. Do not silently reclassify them or fresh candidates as development. Record two-sided size/quality/runtime bounds with rationale before each new experiment family.
2. Next 1–2 hours: reproduce the background probe on the same canvas and compare reconstruction quality and timings, not just extreme-QP byte counts. Wire the existing VVC or SVT encoder into the background stream and standalone client as necessary; prove that selection changes the actual bitstream. Add WebP appearance as the first candidate while retaining JPEG; try AVIF only if supported and the likely benefit justifies the setup. Test round trips, disabled-component behavior, and accounting of headers/metadata/fallback bytes. No raw-source access or untransmitted fitted state at the client.
3. Next 2–3 hours: use verified in-memory PSNR for coarse-to-fine development sweeps over background rate/scale, appearance quality/keyframe interval, and 48/96/192 frames (384 only where annotations and resources permit). Measure full payload bytes, quality, encode and standalone decode time for every point. Pair AV1 and VVC temporal anchors on identical frames, resolution, color conversion and access assumptions; record presets and threading. Do not compare plate-only bytes with full-video anchors. Fix missing manifests or annotations before longer runs. A functioning PSNR path need not wait for piped VMAF. Cache reusable intermediates with input/config/code identities and preserve failed points and explored axes.
4. Next 1–2 hours: investigate the best measured overlapping rate interval. Check mild/severe/unrelated metric controls, ledger reconciliation, repeated timings and source-level uncertainty. Run the full PSNR-Y/SSIM/VMAF/LPIPS suite on a small candidate set; optimize VMAF I/O only if it blocks this step, preserving scores within declared tolerances. Treat out-of-bound good results as alarms. Declare Gate A passed only against its recorded criteria; no extrapolated BD-rate or cherry-picked metric victory.
5. Remaining time: if Gate A passes, freeze a versioned procedure including configuration/selection rule, adaptation budget, anchor settings, metrics, eligibility and manifests. Run Gate B only on verified reserved sources. Per-video fitting of evaluated frames is encoding and is allowed under the fixed procedure with complete byte/time accounting. No manual tuning after seeing confirmation outcomes. If confirmation fails, preserve it as a failed test; any subsequent redesign makes those sources development data. If insufficient fresh sources exist, leave Gate B open and advance ablation controls, neural-baseline setup and receiver profiling on development data. Preparation can overlap gate order; claiming later gates passed cannot. If Gate A still fails, use the remaining time for a targeted bottleneck-led search and document the tested boundary; do not switch to unbounded generative training.

Run long jobs detached, log progress at least every ten minutes, checkpoint at least hourly and verify resume before relying on it. Bound per-point runtime and memory. Confirm results produced versus entries submitted; exit zero is insufficient. Monitor running jobs and continue useful work while they execute. Avoid starting a run that cannot checkpoint before the budget ends. Use existing compute only; if a capability is unavailable, record it and continue independent work.

Apply relevant test/measurement skills. Explain the focused behavioral test cases and proceed under this prompt's authorization to write those tests; do not wait overnight for approval of routine tests or reversible fixes. Run project verification from docs/setup.md for code changes, inspect CI, and keep one PR per independently revertible implementation change. Merge only when validation supports it; do not force-push or remove other sessions' worktrees.

Before stopping, commit/push results and fixes, put exact commands, input/config hashes, output locations, failures, validation and CI status in the PR, and update the owning areas and PLAN.md. Leave no result solely in chat or an untracked scratch file. Report actual elapsed work, completed/remaining gates, the best supported size/quality/speed result with uncertainty, all unresolved alarms, and the next executable step. If jobs remain, include their PID/session, log, checkpoint, output path and resume/monitor commands. Do not promise an overnight win or mark a gate passed because the time budget ended.
