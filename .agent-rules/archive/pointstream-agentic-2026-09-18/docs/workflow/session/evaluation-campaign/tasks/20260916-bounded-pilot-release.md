# Conditional pilot release — 16 September 2026

Return to **Clean project state and dispatch**, task
`01a0a923-4c5e-71c3-8993-5c68f2a76bb4`.
This brief supersedes older next-run cards where they disagree. Codex reviewed
worker heads #117 `d700a93`, #118 `e73c8ba`, #119 `745271f`, and #121 `bc1a144`.
#117 is accepted and merged as `015c971`; its exact-head CI passed.
The other heads have passing CI but their execution cards are not accepted as
written. The repairs and conditional pilots below are authorized now. Workers
may proceed without another card-only return when their listed gates pass,
required checks are green at the actual launch revision, and corrected changes
are merged. Stop and report a gate that cannot be satisfied within budget.
Do not infer scientific acceptance from this execution authorization.

## Shared boundaries

- Fetch fresh `origin/main`; require `015c971` ancestry and pin the actual code
  head, source stack, serialized payloads, native binary paths/versions and configs.
- Keep data outside code. Reuse the saved E03B four and E04A six bitstreams;
  zero conventional or removal-OFF background re-encodes. New directories only:
  refuse an existing run directory before writing bounds or any artifact.
- Federer/Djokovic scene 007 is exposed **development**, never confirmation.
  Quarantine every source/scene in
  `manifests/evaluation_20260916_coordinator_confirmation_reservation.json`.
  No confirmation scoring, training, parameter selection or source replacement.
  Exposure/PTS audit and final selection freeze are still open.
- Use setup/resource-claim rules, any actually free GPU, CPU <=90% available,
  and whole process-group deadlines. No hardcoded device takeover. Preserve
  paused worktrees; disable the unsafe automatic retirement hook for merges.
- Register plausible two-sided bounds with rationale before new scores. Calibrate
  identity, mild/severe degradation and unrelated/null on each claimed metric
  scope. A failed control excludes dependent claims; an out-of-band observation
  is an alarm to investigate, not automatic proof of collapse or leakage.
- Report actual charged bytes, quality and sender/client runtime, including
  preprocessing and hardware strata. Distinguish measured from missing timing.
  At n=1 development scene, report exploratory paired effects; do not manufacture
  independent samples or source-level standard errors from correlated frames.
- No BD-rate on the nonoverlapping AV1/VVC ranges; no broad codec, ghosting
  elimination, generalization or cross-host speed claim from these probes.

## Cursor — E06 four-setting implementation and diagnostic

**Action:** evaluation / E06. Worktree
`/home/itec/emanuele/worktrees/pointstream-e06-20260916`, branch
`codex/e06-20260916`. Continue #121 rather than splitting preparatory replies
into new PRs. Own E06 manifest/runner, its relevant tests and evaluation state;
leave generator trainers and E04 background implementation to Antigravity.
Preparation cap: two active hours. Execution cap: 30 minutes total, four new
PointStream settings only, with at least one third reserved for decoding,
scoring and ledger verification; stop if one setting exceeds seven minutes.

Question: does the medium residual measurably improve each predictor, and is
its fully charged rate/quality competitive with the saved conventional neighbours?
Reuse the exact 48x360x640 RGB stack, four conventional streams/decodes, and
registered-panorama QP47 bitstream plus side data. Check actual SHA-256 files,
not abbreviated report text; correct copied digest errors if present.

Before launch:
1. Implement an executable runner: #121 currently refuses `--launch`. Serialize
   actual appearance, masks, placements, residual and all headers/envelope data.
   Reconcile the complete transport length with T=B+F+M+R+H; `16 * placements`
   and assumed zero envelope bytes are estimates until bytes are actually written.
   Include the background's 1,742-byte side payload exactly once. A client must
   reconstruct using only transport and declared shared software/model state.
2. Name predictors honestly: the current “paste” sends current-frame crops;
   “warped-reference” resizes a first crop into later union bounding boxes.
   Label them `per_frame_crop` and `bbox_resized_first_reference`, respectively,
   unless an actual geometry warp is implemented and proven invoked. Do not call
   this optical flow, per-object pose warping, or the E05 first-reference paste
   control. Record union-mask/multiple-player limitations and `pose_present=false`
   when pose is not serialized. Keep the grid at four settings.
3. Require strict decode counts/dimensions, ordinary/standalone pixel equality,
   source/cache hash equality, overwrite refusal, residual-off bytes/calls=0,
   and residual-on R>0 plus demonstrated pixel change. Regression checks must
   exercise malformed transport/counts and complete ledger reconciliation.

Conditional launch: each predictor residual off/on, WebP quality 50 and the
existing medium residual rung (AV1 rate 45, block 8, threshold 4, background
scale 2), generation off. Preserve card bounds/calibration floor dispositions.
A >=0.5 dB paired improvement is a residual-effect signal, not evidence that its
rate cost pays. Report all T/quality points against actual neighbours; a ±0.5 dB
match alone cannot certify a win. Claim only measured dominance within declared
indifference bands or explicitly bracketed interpolation without extrapolation;
require other quality metrics within their bands and report speed separately.
No overlap means inconclusive competitive evidence, not permission for a ladder.
Return artifacts/hashes, alarms, charged frontier and promote/stop disposition.

## Antigravity foreground — E02S acceptance repair then E05 Stage 1

**Action:** generation / GEN-ACT-12, E05. Worktree
`/home/itec/emanuele/worktrees/pointstream-eval-e02r`, branch
`codex/eval-e02r`. Continue #118, owning reference/trainer/campaign code, the E05
card and generation state. Cursor owns result adapters; use their real public
function API rather than editing them. Preparation cap: two active hours.
Execution: one GPU, **aggregate <=1.0 GPU-hour**, including training, diagnostics,
validation and any ambiguity extension. Reserve at least one third for evaluation;
any <=0.5-hour extension fits inside that total, never adds to it.

Reference-first and invalid-value repairs are accepted in principle. CPU CLI
continuation proves G/D tensors and tested Adam moments exactly equal; no CUDA
continuation is claimed. Do not say different frame positions guarantee unequal
pixels: the required rule is that reference selection never reads target t>0.

Before launch:
1. Correct the card's premature “authorized” status, development/confirmation
   labels and executable commands. The shown trainer command selects no tiny
   scene/window; its default workers and batch also disagree with the protocol.
   Materialize a bounded development-only dataset view or add a verified subset
   selector for 16–32 training frames of alcaraz_highlights/scene_028. Enumerate
   exact training/evaluation item and track IDs; show actual loader contents and
   disjoint target frames before any training. Use batch 1, workers 0, first
   reference; shared first references must be explicit in the split policy.
2. Use flags supported by the actual diagnostic parser (`--video`, `--scene`,
   `--checkpoint`, `--output`, control flags). Its shown `--weights`,
   `--scene-path`, `--output-dir` and `--reference-mode` do not exist. The adapter
   module has no executable CLI. Provide a checked helper calling its real API,
   and verify the validation window is genuinely selected (not the default first
   frames). Record an argument/subset smoke check, not just an example command.
3. Enforce one elapsed deadline around the entire process group, including
   evaluation. Use atomic checkpoints and ten-minute log heartbeat. Replace
   unsupported quality/rate bounds with justified bands including historical
   low-quality generator outcomes and transmitted checkpoint size. Missing total
   rate must remain incomparable; residual-only bytes cannot substitute for total
   wire bytes in candidate promotion. Include container/header bytes.
4. Specify object fidelity/appearance and temporal diagnostics alongside whole
   frame scores, conditioning/null/residual-off controls, actual pasted first
   reference and genuinely supported warp controls. Expose any unavailable
   baseline explicitly. Separate practical client resource budget from a 5%
   measurement indifference band. Promotion cannot tolerate worsening the other
   quality axis outside its band merely because one axis improves.

Conditional launch: prioritize one pix2pix tiny-scene fit first, then disjoint
**development** validation only if learnability/control gates pass. Start fresh
under the corrected reference policy; legacy shortcut checkpoints cannot promote.
This learnability pilot does not require a codec win in advance. Expand to another
family only with time remaining inside the same cap and a recorded discriminating
question. Preserve unsuccessful families and model costs. If subset/evaluator
cannot be made real, return that blocker without launching a broad training run.
Report loss/control evidence, full-byte/quality/client results and remaining gap
before Stage 2; no Stage 2 release is implied.

## Antigravity background — E04 evidence repair then two removal-ON points

**Action:** codec / CODEC-ACT-07, E04 paired removal. Worktree
`/home/itec/emanuele/worktrees/pointstream-e04a-evidence-20260916`, branch
`codex/e04a-evidence-20260916`. Continue #119, owning evidence completion,
background probe/tests/proposal and codec state. Preparation cap: two active
hours; execution cap 30 CPU minutes including calibration, decode and scoring,
<=16 threads within the shared cap, zero GPU. Exactly two new panorama encodes,
scene 007 removal ON at QP47/32; optional still/camera arms are not released.

Before launch:
1. Propagate failed calibration into runner control flow and derived eligibility.
   Currently main prints `valid=False` and continues; derived records always say
   `metric_calibration: verified`. On failure, retain the alarm report and halt
   dependent scoring/validated output. Test the runner's failed-control path,
   not only the calibration helper; apply absolute/identity/order/null checks to
   every metric scope being certified and capture empty-mask warnings.
2. Refuse existing evidence directories before any write. Put corrected derived
   artifacts in a new revision directory; preserve original evidence and existing
   reports. Prior valid artifacts do not acquire repaired guarantees silently.
3. Build the reused-arm table from canonical immutable reports, configuration
   and bitstream/side hashes, never hardcoded strings. Four entries in the latest
   proposal disagree with the original report (still32, panorama32, video47,
   video32). Verify same RGB/mask stack and actual removal-OFF configs. Record
   previously overwritten derived evidence as such; do not reconstruct history
   by silently rewriting it. Rename the proposal as a same-camera paired probe.

Conditional launch: reuse all six original OFF arms and create only two ON
panorama points. Demonstrate real mask exclusion/fill invocation and contrasting
zero OFF calls. Decode standalone from serialized geometry/bitstreams, reconcile
side bytes, compare on identical fixed regions and preserve visual samples.
Use the proposed >=50% MAD reduction / <=0.5 dB visible PSNR loss / <=15% **total**
byte increase as exploratory decision bands, with sender and client runtime too.
MAD includes background reconstruction error; it alone cannot prove elimination
of silhouettes. Report boundary artifacts and visual evidence, and mark intermediate
outcomes inconclusive. Scope keep/promote decisions to this scene and budget.
The 15-second/50-KB estimate is not a cap or full storage accounting: include
saved RGB decodes and reports. A second camera remains a later costed gate.

## Coordinator submission work

Paper setup commit `7a0476e` is pushed and builds to 28 pages; no result holes
are closed. Neural source/input pins remain available, but weights and isolated
compatible environment/extensions still block a verified neural round trip.
Keep final procedure unfrozen until development identifies a credible regime;
then audit exposure/PTS, freeze candidates/splits/analysis and request the held-out
confirmation stage, followed by required ablations and second domain. These
requirements remain open, not tasks secretly assigned to the background worker.
