# Component strategy checkpoint — 17 September 2026

Return to Clean project state and dispatch, `01a0a923-4c5e-71c3-8993-5c68f2a76bb4`.
The campaign strategy and submission requirements are unchanged. Components and
controls precede assembly; a credible development advantage precedes frozen
confirmation, then second-domain evaluation. Preparation can run in parallel.
Current search coverage is narrow; evidence repairs do not replace screening
resolution/fps, colour, rate, reference cadence and duration mechanisms.

## Decisions from the latest returns

- E06/#128 head `4da57f1`: three derived artifact hashes match the report, parent
  quality is inherited under pixel equality, and exact-head CI is green. Accept
  the packaging/client evidence as a single-scene diagnostic and stop this tested
  configuration as a codec-win candidate. No BD-rate, generalization or sender
  live-operation claim. Public claim helper still needs narrow review: one sample
  is not repeated timing; model deployment declaration alone does not reconcile
  weights into total physical wire cost. Do not merge until these gaps are fixed.
- E05/#129 merged `7b771f0`: completed matrix/adapted hashes match; blank and repeat
  controls are present. Accept no Stage2 promotion for this checkpoint. Preserve
  conditioning response as a single-scene diagnostic; no architecture-wide
  rejection or complete calibration claim. The completed files do not include
  the full object/temporal calibration record. The fitted model remains a charged
  deployment obligation. Do not spend more GPU budget making this loser citable.
- E04B/#127 head `75f2d95`: exact-head CI is green and derived artifact hashes match.
  Retain removal-OFF provisionally for this scene; no Telea effect was exercised.
  Audit source hardcodes retry durations, historical cause/provenance and final
  disposition. Numeric OFF checks use tolerance, not bit-identical metric proof.
  Empty regions return zero, which is not measured zero error. The later <20%
  policy is an explicit post-result decision, not a pre-registered stop rule.
  Do not accept global geometric-limit or complete alarm-closure claims. Keep
  original bound violations and their separate diagnostic disposition visible.

## Next bounded preparation, not another execution wave

Each worker: at most two active preparation hours, no new native candidate
encodes, GPU runs, training, confirmation scoring or second-domain scoring.
Read root/host rules, this brief, the campaign plan and the named owning area.
Keep existing branch/worktree; preserve immutable artifacts and paused trees.
Code fixes need relevant behavior checks and green CI at the returned head.
Return a paste-ready report and pause; no automatic next-stage launch.

### Cursor — finish #128 acceptance and propose a discriminating system probe

For `derive_claim_eligibility`, require actual repeated client measurements for
speed; a single sample remains descriptive timing only. Fail closed for invalid
metric domains/non-finite values, and separate residual-byte diagnostics from
complete total-wire claims. For fitted models, require evidence tying any charged
weights to the reported physical total; a positive declaration is insufficient.
Shared-model availability and amortization need named supporting evidence.
Preserve valid RD-only rows when client evidence is missing.

Then return a costed card, not more saved-arm timing: identify the minimum mask,
placement/framing and predictor/residual costs needed to cross the saved anchor
budget at comparable quality. Use existing payload inventories and lossless
auxiliary-size estimates where possible. A compact stream or longer amortization
window is only worth testing if the remaining physical bytes plausibly fit and
prediction quality has headroom. Name competing explanations, one changed factor,
controls, <=6 settings, <=30min execution proposal and stop/promote rules before
scores. Do not call union-bbox resizing a per-object pose warp.

### Antigravity foreground — costed route to a baseline-clearing candidate

Accept the present checkpoint's stop. Use existing train IDs, loss records,
reference parity and controls to distinguish tiny-fit learnability from disjoint
appearance prediction. Supply evidence paths for claims already made; keep
missing object/temporal calibration explicitly missing rather than launch more
runs. Do not manually upgrade eligibility booleans.

Return two bounded alternatives with a recommendation: a correctly invoked
pretrained/shared foreground model with honest receiver/corpus assumptions, or
one architecture-informed change to this reference/pose prediction path. State
why it could beat paste and a genuine supported warp with residual OFF; give
model wire/storage cost, training/dev split, controls and actual expected budget.
No training is released. No model-family grid or Stage2 promotion.

### Antigravity background — narrow #127 acceptance correction

Use retained commands/logs to support retry invocation counts/durations and map
run-time source to committed file hashes. If historical evidence is absent,
label it reported/unverified; do not generate a ledger from hardcoded narrative.
Read canonical values and policy from preserved inputs, compute deltas and scoped
verdicts from actual arrays, and refuse overwriting derived outputs. Label
reproduction with its actual tolerance. Empty masks mean unavailable metrics,
not zero error; cover this behavior with relevant tests.

Keep observed uncompressed-plate error distinct from a proven geometric lower
bound. The causal split among registration, noise and lighting is unresolved.
Mark the <20% disposition as a later scoped policy decision, preserve original
alarms and link calibration evidence for each claimed metric. Do not seek a
second camera or more removal arms yet. This task closes reporting integrity;
it is not another background representation search.

## Coordinator work and roadblock

Overleaf main is verified at `18d4778`. Neural anchor source/input and RT native
entropy toolchain are prepared; weights remain inaccessible via official shell
links, and no neural video benchmark exists. Confirmation media hashes match and
retained-scope source-bound score scan has no flags, but deleted/unrecorded uses,
manual replay/duplicates, decoded-frame preroll and final procedure freeze remain
outside acceptance. Scoring is unauthorized.

September17 arrives without a valid baseline-clearing generator: this is the
campaign's explicit roadblock checkpoint. Alternatives are a bounded mechanism-
driven development probe, or an explicit scope/date decision if no plausible
candidate can clear the gate. The current robotics demo is provisional task-
utility evidence against selected starved AV1 arms; it is not an accepted substitute
for frozen tennis superiority or the later second-domain requirement. September20
remains a scope/date review, not permission to omit these requirements.
