# E07 — Independent confirmation and secondary-domain boundary

Owner: Cursor. Areas: [data](../../../../areas/data.md) and
[evaluation](../../../../areas/evaluation.md).
Depends: E06 frozen tennis advantage; E01 must reserve sources earlier.
Scope: confirmation/domain manifests, protocol drivers, result analysis; no
score-driven model changes inside the same frozen confirmation run.

## Work

Use the prospectively accepted source-count policy and actual untouched match
identities. Six preferred, fewer permitted for deadline with stated rationale;
never recycle the two previously scored candidates as fresh. Verify exact
code/config/weights, selection/adaptation budget, quality/latency floors, formats,
rate ladder, identity/metric/wire controls and timing-strata coverage before test.
Do not reduce the source count after viewing a losing source or uncertainty.

Run the fixed procedure on separated scenes per reserved match and report each
source plus aggregate uncertainty. Frames/scenes nested in a match are not new
independent matches. Use small-sample-appropriate intervals; do not assume the
existing normal 1.96 interval establishes significance for two or three sources.
If uncertainty is too wide, state inconclusive and cost an extra-source option.
Configuration changes after confirmation require a new recorded evaluation
protocol and fresh holdout or an explicitly exploratory claim.

After the tennis advantage is established, take one available secondary domain
(surveillance/conferencing or justified alternative) and a bounded matched
anchor/PointStream comparison spanning a contrasting motion/background regime.
Reuse acquisition screening first; avoid a new broad data collection campaign.
Keep the tennis-selected configuration fixed first; any domain adaptation is a
separate declared experiment with its training/time/weight cost. Retain negative
results to explain domain limits. Secondary-domain superiority is not required,
but a defensible comparison and bounded claim are.

## Acceptance

Frozen manifest and validated standalone source-level comparisons, honest
confirmation verdict and secondary-domain figure/table with all three axes.
Update the selected count/protocol evidence without falsely passing legacy
six-source gates. Return every unresolved submission requirement and exact
artifact list for E08. Do not declare success merely because all jobs finished.

## Start, validation and return

Read [campaign plan](../plan.md), your named area, and the project
[session workflow](../../SKILL.md). Follow its worktree, ownership, reuse-first,
bounds, per-stage budget, scheduling and return contract. Output root is
`$PS_DATA_ROOT/outputs/evaluation-20260914/<task-id>/<unique-run-id>/`; job records
are under `$PS_DATA_ROOT/jobs/`. These paths are proposed destinations, not
existing evidence. Commit code/docs, run relevant behavior tests and setup
verification plus CI; native paths require a real encode/decode control.
Documentation-only work uses link checks and `git diff --check`. Report exact
validation commands actually run; do not invent working CLI flags in dispatches.
Return here after the bounded assignment; wait for the coordinator's next task.
