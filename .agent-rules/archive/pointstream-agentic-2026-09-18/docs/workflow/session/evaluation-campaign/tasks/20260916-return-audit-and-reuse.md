# Pilot return audit and reuse assignments — 16 September 2026

Return to **Clean project state and dispatch**, task
`01a0a923-4c5e-71c3-8993-5c68f2a76bb4`. Supersedes next-action instructions in
20260916-bounded-pilot-release.md; preserve that brief as the launch protocol.
Audit baseline: `c16af58` on fresh shared main; E06 launch head `def647a`,
#121 merged `c656ee6`; #119 merged `aefdeb2`; #118 merged `742dffb`;
#125 documents the E05 pilot. E04B source exists on local branch
`codex/e04b-paired-removal-20260916` at `d30e59f`, not yet shared main.

## Accepted boundaries and open evidence

E06 report SHA-256
`52c0179c724469e56a1dedeafd8695536b44623cd0a63c48bf8128a49f8b5e33`, bounds
`0c88b4a360220d452faa540f1ea499a403fdba86990c859466b2a685aceb2230`, rows
`7bab384fee466c580256f8b9ce14a6409888fadbf97e1c397a9b83dda6ce3c68` match.
The four charged ledgers and residual invocation/pixel checks are useful
exploratory evidence. Accept stopping this tested full-codec configuration as a
competitive result; a residual-effect signal is not a codec win. Separate client
runtime is missing: only combined setting/scoring seconds are stored. Calibration
is a reused, hash-pinned two-frame same-grid record; report summary anchor fields
are null despite valid nested source anchors. Carry the actual calibration scope.

E05 matrix SHA-256
`1baa1dda6e32c35e7c9c22bcb171181db342a8d93ad6e36de1d82d2522aaf0ed` and adapted
result `f56e5cb2a8c0239b4f3ecc007d164a403cb7512d8a6bd4ed1e625afdf7c8a425`
match. Keep no Stage 2 promotion, but do not certify complete controls or declare
pix2pix inherently weak. The record says `no_conditioning_tested=false`; object,
temporal and scorer-calibration evidence are incomplete, and the wire bound
fired. A parser-only reproducer finds both `--no-conditioning-control` and
`--no-no-conditioning-control` leave the value false. First appearances are
resized into later bboxes; different conditioning appearance hashes alone are
not target leakage. The trained checkpoint is 217,736,406 bytes, while the ledger
charges only pose/motion and generator identity metadata, without a declared
shared-model/deployment-cost policy. A checkpoint digest is not delivered weights.
Adapted `rd_claim=true`/`speed_claim=true` do not close these campaign requirements.

E04B's r2 report has the expected RGB/mask and canonical OFF report identities,
but two open ghost-MAD alarms. Registration/parallax/noise as the dominant floor
is an untested causal explanation. Zero inpaint holes means Telea was not exercised.
Boundary-MAD fields default to 0 when missing, so those are not measured zero
boundary errors. Code revision records `aefdeb2`, dirty=true and a diff digest;
complete actual-source provenance. Both original and r2 directories contain ON
bitstreams; reconcile copy/retry history and cumulative budget before asserting
exactly two encode executions. No further candidate encode is released.

All results have one development scene per probe, not independent confirmation.
Reserved sources remain untouched and scoring unauthorized. Native-seek/PNG
provenance stays unresolved for 47/48 scene007 frames. No BD-rate on the current
nonoverlapping AV1/VVC ranges. No second camera/domain or training ladder release.

## Cursor — lossless transport audit and missing client stratum

**Scope:** evaluation E06, client serialization and result adapters; continue an
isolated `codex/` branch in the existing E06 worktree. Coordinate changes through
public interfaces; Antigravity owns generator trainers/evaluator and E04.
Preparation <=2 active hours; CPU execution <=30 minutes under claims. **Zero new
native candidate encodes**, zero training or conventional/background re-encodes.

Reuse the four saved E06 `transport.npz` files and their exact model-free client
reconstruction. Before new timing, register host/workload bounds and provenance.
Profile fresh client deserialize/decode/render separately from sender serialization
and scoring, with warmup and repeated compatible workload timings. Hash and compare
all delivered pixels and strict dimensions/counts; never overwrite originals.

Audit lossless packing using one baseline plus one explicit compact representation:
compress repeated envelope/placement/mask data and avoid redundant stacks while
retaining exact arrays and client semantics. Charge every header and compression
byte; decompression is client time. Do not re-encode native appearance/residual/
background bitstreams, rescore unchanged pixels, or expand a rate ladder. Original
bytes are historical; new packaging creates distinct derived records with parent
hashes, pixel parity and honest ledger allocation (compressed wire bytes differ
from uncompressed logical array sizes). Do not subtract logical bytes from compressed
wire length and manufacture an overhead remainder. Record pose absent and union
predictor limitations. Simple whole-envelope compression can reduce overhead but
cannot remove the existing residual payload cost. Report exact payload floors;
stop the codec search here if even optimistic complete charges cannot compete.

Repair claim eligibility so missing campaign-required calibration/blank controls,
undeclared model deployment costs and absent timing cannot be silently upgraded
by producer/adapter booleans. Preserve RD-only records with genuinely valid RD
when speed alone is missing. Do not mutate old output JSONs; create derived audit
records excluding unresolved claims. Supply the foreground worker a compatible
adapter/helper revision; do not rewrite its training/evaluator files.

## Antigravity foreground — complete the saved-checkpoint pilot evidence

**Scope:** generation GEN-ACT-13/E05, evaluator/control flags, diagnostics, E05 card
and generation state. Existing worktree now uses `codex/e05-stage1-pilot`; fetch
fresh main, pin revisions and keep Cursor adapter ownership. Preparation <=2
active hours. **No new training or model-family grid.** CPU reuse/calibration <=30
minutes. GPU corrective execution <=30 minutes AND <=verified unused portion of
the original aggregate one GPU-hour; all pilot attempts/diagnostics count. If no
budget remains, return the minimal artifact gap rather than extending the cap.

Fix the BooleanOptionalAction flag semantics with an unambiguous positive control
option and supported legacy behavior. Show parser and executed control tests;
blank conditioning must produce a real additional corner, not an enabled label.
The six-corner saved report cannot be retroactively called seven-corner evidence.
Reuse saved checkpoint, source window and any saved transports/decodes. Rerun only
missing controls or outputs needed for a verified inference path; no full matrix
repetition when matching corners can be reused by actual identities.

Audit train item/track IDs, evaluation frame coordinates and reference availability;
show recorded losses/gradients and training samples before asserting learnability.
Confirm server/client receive the same decoded first reference per object, not
an uncharged raw appearance. Calibrate object-region fidelity and whole-frame
metrics on matched controls; add appearance/temporal diagnostics, blank and
supported reference/warp controls, explicitly marking unavailable baselines.
A pixel hash difference proves invocation/sensitivity, not useful pose fidelity.

Resolve the existing wire alarm using actual ledger/config data and replay, never
by silently widening bounds. Declare deployment regime: per-video fitted weights/
adapters must be transmitted and charged; a fixed shared model needs an explicit
training provenance, receiver availability and amortization policy. Report model
storage/setup time separately even in shared mode. The tiny scene-fit checkpoint
cannot become free shared state solely to improve the result. Add dependency-safe
derived eligibility with Cursor's public API and preserve original artifacts.

Return a scoped no-promotion/learnability verdict with object/control evidence and
full size/quality/sender/client strata. No Stage 2 until a valid neural candidate
clears declared baselines. Keep pix2pix/SPADE families available; a flawed or losing
pilot does not prove a family cannot help.

## Antigravity background — close alarms and code/retry provenance by reuse

**Scope:** codec CODEC-ACT-07/E04B, its script/tests/derived reports and codec state.
Use the existing E04 worktree/`codex/e04b-paired-removal-20260916` branch. Commit,
push and open/update a PR for actual E04B source with exact-head required checks;
merge only after these evidence gaps are addressed. Preparation <=2 active hours;
CPU decode/rescore/control budget <=30 minutes under <=16 threads/shared cap.
**Zero new candidate encodes**, including retries, second camera or optional arms.

Preserve both run directories. Inventory native commands, timestamps, stream hashes
and failed/copy/retry history; report cumulative time and actual encode invocations.
Link the dirty run's recorded diff digest to exact committed source/file hashes;
if full reconstruction is unavailable, keep that provenance explicitly incomplete.

Close each alarm through the same analysis path on canonical OFF saved decodes:
reproduce original metrics, verify same RGB/mask/ghost-region definition and
scorer calibration, then separate lossless uncompressed plate reconstruction,
coded reconstruction and mask-exclusion effects using saved/prepared arrays.
This can support a revised diagnostic bound with rationale and immutable alarm
disposition; it cannot prove parallax/noise dominance without a discriminating
control. Keep the causal explanation a hypothesis if the components cannot be
isolated cheaply. No claim about Telea effectiveness when there were no holes.

Replace missing boundary MAD default 0 with missing evidence, or compute and
calibrate the actual boundary quantity on saved decodes. Complete absolute,
identity/order/null checks for each certified region metric. Reconcile written
promote/stop/inconclusive rules with evaluated code, including outcomes below20%
reduction; do not silently change policy after reading results. Calibrated outcome
alarms and decision criteria are different checks. Return a fresh derived report,
visual audit and scoped disposition; no new win or second-camera claim implied.

## Coordinator remaining submission work

The manuscript's evidence-status commit is `18d4778`; rendered text and
HOLEs are unchanged. The authorized push is complete and Overleaf main verified
at that commit. Paper remains 28 pages with no spare budget.
Official UF, RT and all-model backup weight folder links were rechecked: all
return HTTP403 in shell. No checkpoint is available; RT also documents a separate
Python3.12/PyTorch2.6/CUDA12.6 stack, so it is not a drop-in fallback.
Codex owns neural-anchor installation/weights/round-trip readiness and the exposure/
PTS audit, while these workers finish evidence reuse. A recent neural anchor,
valid foreground baseline clearance, a credible development regime, frozen
confirmation and the required second domain remain submission gaps. September20
is a scope/date checkpoint; these negative/incomplete returns do not pass it.

Coordinator update: RT CPU entropy extension builds in isolated Python3.12 and
round-trips 384 synthetic symbols in fresh processes (100 bytes). No neural video
encode/decode or weights; downloads remain blocked. The
[readiness audit](../../../../../manifests/evaluation_20260916_coordinator_readiness_audit.json)
records intact media hashes for three reserved sources and no source-bound codec
score flags in 1390 retained text records under its binding heuristic. All 156
reserved bounds were checked against packet keyframe start. Decoded-frame preroll,
manual replay/duplicates and selection freeze remain open; scoring unauthorized.
