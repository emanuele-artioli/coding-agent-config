# Evaluation coordinator handoff — 15 September 2026

**Trigger:** the user explicitly requested a session-length/context handoff after
receiving new E03A/R0R/E02S reports. This is a clean coordination boundary, not a
GPU outage, scientific stop decision, or permission to replay earlier campaigns.
The latest worker changes have NOT had final coordinator acceptance. Resume by
reviewing the three open PRs, not by issuing another generic repair wave.

## Overall task and user decisions

PointStream is an object-centric video codec targeting ACM TOMM submission on
**30 September 2026**. The provisional **20 September evidence freeze** remains;
review feasibility explicitly if it slips. Find and confirm where PointStream
beats AV1, VVC and a recent neural codec, with at least one neural foreground
method clearing its reference baselines. Then retain a secondary-domain study.
The [campaign](plan.md) and [roadmap](../../../roadmap.md) own detailed policy.

Paper order: setup → anchors → still/panorama/video with removal off → paired
foreground removal/inpainting → neural foreground/reference comparison and staged
training → residual contribution → complete codec versus anchors/domain limits.
Execution may overlap. Low-resolution/low-fps screening comes first, then native
and mixed corners, then one or two intermediate resolution/fps points. Sweep
valid colour paths and other consequential axes without a full Cartesian grid.
Find live/playback feasibility on named hardware; count lookahead/startup and
sustained queues, and retain slower/offline quality candidates.

Resource authorization persists: any GPU free at launch across accessible shared
servers, aggregate campaign CPU allocation <=90% available cores per host, never
preempt colleagues. Use tested claims, detached jobs, hourly checkpoints and
10-minute file progress; actionable events only, no recurring agent polling.
Default first probe <=6 settings /30 minutes including scoring; larger stages
need a costed coordinator decision within already authorized resources, not a
new permission request for every reversible step.

Reuse evidence before launching; preserve all artifacts. Timing may be reused
across defensible hardware/workload strata with uncertainty rather than measured
anew for every RD point. Missing timing does not invalidate valid RD; missing
quality/decode/byte validity does. Keep diagnostics distinct from validated claims.
Six independent confirmation matches is preferred but the user authorized fewer.
Coordinator accepted three fresh matches and their acquisition shortlist; source
eligibility and final scores remain pending. Never substitute exposed Gate B data
as fresh confirmation. One scene's trajectory evidence is not generalization.

## Accomplished and accepted

- Reconciled the September 12 handoff with already-merged #100–#102 repairs;
  preserved historical evidence rather than re-running the old Wave 2 plan.
- Published a main campaign and eight scoped task briefs (#103, `eeb2c81`), then
  acceptance findings/targeted continuation briefs (#106 `b07db0b`, #107 `83c75ed`).
  PLAN, areas, roadmap and experiment/session policies are aligned. All three
  coordinator PRs are merged; #107 CI success was rechecked for this handoff.
- Established the reuse-first policy and discovered that the existing three-mode
  background probe is removal ON; #96 introduced it, #98 concerns generation
  diagnostics, #101 repaired probe transport. BP30 modes are plate-reference
  policies, not the requested three representations. Do not relabel either.
- Acquired three reserved match windows without codec scoring. Coordinator
  independently verified media sizes/SHA-256 and acquisition report/log hashes.
  All files are 1080p; they cannot confirm a native 4K claim. Eligibility is still
  false pending event/overlap, time-origin and content-based scene checks.
- Verified the earlier real E02R artifact hashes and full-trajectory execution:
  the default full-trajectory CLI path was enabled; placements span all 16 frames.
  This replaced the old sparse first-frame diagnostic limitation. It did not by
  itself prove useful conditioning, training readiness or an RD advantage.
- Acceptance audits reproduced real errors before broad campaigns: invalid RD
  ingestion, inconsistent schemas/provenance, missing controls treated as passed,
  epoch-bound checkpointing and resource claims released under live children.
  These drove bounded fixes, not scientific claims that a model family fails.
- Resolved cross-lane ownership: **Cursor exclusively owns the result adapter
  and campaign reader**; Antigravity owns trainers/evaluator and R0 job support.
  Keep this division through integration to avoid another incompatible adapter.

## Latest work reported; acceptance review still due

**E03A / Cursor / #104:** reports finite/domain/control validation fixes,
producer-shaped mapping in the experiment layer, exact artifact file hashing,
correct 4K shape, and honest diagnostic versus validated eligibility. Derived old
probe is kept for diagnostic RD but excluded from validated claims because
standalone decode/calibration remain unverified. Contract pin is
`e03a-20260915`; anchor card prepared but not launched. A tiny native encoder
roundtrip was reported, not the real 48-frame anchor batch.

**R0R / Antigravity / #108:** PR body reports conservative liveness/token recovery,
child process-group termination before release, failed-query rejection,
cgroup/load-aware CPU allowance and mandatory job CPU claims. User-pasted report
instead describes a **15-second heartbeat lease**. This disagreement is an
explicit review question, not a newly confirmed bug: inspect the actual PR head
and prove expiry alone never permits takeover of a live/unverifiable owner.

**E02S / Antigravity / #109:** reports deterministic epoch sampling and CPU RNG
restoration, actual interrupted/resumed optimizer continuation matching an
uninterrupted control, seven diagnostic corners including same-seed and blank
conditioning, and rate/quality/resource selection with practical indifference
bands. It excludes Cursor-owned adapters. New runtime/control evidence has NOT
been independently inspected by the coordinator. Do not copy its quality table
into paper results or treat bit-identical hashes alone as useful pose fidelity.

The latest reports contain real progress, but no accepted baseline-clearing
neural model or full-codec SOTA regime. Gate A is open, held-out confirmation
incomplete, E04 comparison outstanding, E05 campaign unreleased, and final paper
results have not been written. Much of this session made trustworthy experiments
possible; the main scientific comparisons are still ahead.

## Verified repo / PR / worktree snapshot

Snapshot checked 15 September; refresh before action. All listed worktrees were
clean (no tracked/untracked changes). No worker branches were merged during this
handoff. All three PRs were **OPEN**, with lint/types/tests green at these heads:

| PR / branch | Exact head | Worktree | CI run |
|---|---|---|---|
| [#104](https://github.com/emanuele-artioli/PointStream/pull/104) `codex/eval-e01` | `9d563030492efef350a4505c7e71ad449bea1ada` | `/home/itec/emanuele/worktrees/pointstream-eval-e01` | `34904242545` |
| [#108](https://github.com/emanuele-artioli/PointStream/pull/108) `codex/eval-r0` | `287c2eb1785ae5abdb90b3e5e2ea00a1c57fb244` | `/home/itec/emanuele/worktrees/pointstream-eval-r0` | `34905548969` |
| [#109](https://github.com/emanuele-artioli/PointStream/pull/109) `codex/eval-e02r` | `013a4c8b0d2ff343deacc801e8c16a195564c807` | `/home/itec/emanuele/worktrees/pointstream-eval-e02r` | `34908449158` |

Main checkout `/home/itec/emanuele/pointstream` was `83c75ed` before this
handoff-docs commit. Fetch shared main to obtain this file's final revision.
Merged #105 `2f63ae1` contains partial earlier readiness code; new worker fixes
are on their PRs, not yet main. The clean old coordinator worktree
`/tmp/pointstream-next-probes` is at `60c52a8` (`codex/eval-next-probes`, squash
merged via #107); do not reapply it. This handoff worktree is
`/tmp/pointstream-handoff-20260915`, branch `codex/evaluation-handoff-20260915`.
Do not remove worker worktrees without checking preservation/paused sessions and
required user approval; never run the prohibited cleanup helper.

Paper repo `/home/itec/emanuele/pointstream/67a9ea6275d3d9785ce57026` is clean at
`55e4bc4f2328647e004ee87143f97bc24de01b98`. No manuscript changes in this campaign
coordination session. Read its own AGENTS.md before edits; commit separately.

## Evidence and landmarks

Data root: `/home/itec/emanuele/pointstream-data`; never symlink into code.

- #104: `manifests/evaluation_20260914_e01r_acquisition.json` points at media,
  report/log and verified hashes. External report:
  `outputs/evaluation-20260914/e01r/acquisition/acquisition_report.json`.
  Reserved IDs: `conf_cand_01_sinner_medvedev_ao2024`,
  `conf_cand_04_medvedev_djokovic_usopen2023`,
  `conf_cand_05_swiatek_sabalenka_madrid2024`. No substitutes used.
- #104: `manifests/evaluation_20260915_e03a_confirmation_timestamps.json`
  records reported stream-copy I-frame preroll; validate frame/time mapping when
  extracting selected scenes. Acquisition windows were fixed 3600–4500 seconds,
  not selected on codec quality.
- #104: `manifests/evaluation_20260915_e03a_contract_pin.json`. Check module
  digests against actual head before accepting the pin: its `code_head` still
  names `7e4da6e` while current PR head is `9d56303`. This was observed during
  handoff, not yet resolved; do not assume pin name alone establishes identity.
- #104: `manifests/evaluation_20260915_e03a_anchor_card.json`: one development
  scene (`federer_djokovic/scene_007`), 48 selected frames, short edge 360/12 fps,
  AV1 preset 0 and VVC slower, QP 63/47 each, four settings, 30-minute cap. Read
  the card and verify paths, exact timing/bounds and calibration before release.
- Existing real diagnostic:
  `outputs/evaluation-campaign/e02r/diagnostic_matrix_pix2pix_scene028.json`, SHA
  `cec034102a3e00c2073158807278312ea3f7ccf56221918331e86dc308af7426`.
  Old campaign record SHA
  `296dc0c98dc441ab85676cf3d4950b58ff35b6e2ce61f3ae97c2e9db9151df84` stays invalid
  as stored. Preserve both.
- New #104 derived record (reported, verify before use):
  `outputs/evaluation-20260914/e03a/derived-from-e02r-diag/campaign_result_pix2pix_scene028.e03a.json`, SHA
  `6d21a16ab307295465804f0bb3c66b1c75f05d8eb1b281aa078d64cd2ea1f743`.
- The latest #109 report/PR body does **not give exact durable artifact paths,
  commands or digests for its new seven-corner and trainer-continuation runs**.
  Locate those on the execution host/worker worktree or request those specific
  pointers from Antigravity; do not rerun merely because the summary omits them.
- [E03A](tasks/03a-contract-to-anchors.md), [R0R](tasks/00r-claim-lifecycle.md),
  [E02S](tasks/02s-training-and-controls.md) contain the previous reproduced
  blockers and narrow acceptance expectations. Treat them as targeted regression
  cases, not instructions to assume the newer PRs still fail.

## Running / queued work and operational limits

This coordinator launched no training or codec experiments; its CPU metadata and
review probes finished. Worker reports say their probes completed and E05 is
unlaunched. No new dispatch is issued by this handoff. A bounded scan of shared
`jobs/**/status.json` found only older completed/failed Wave 2 records, which does
NOT establish other servers idle or new detached jobs absent.

Local environment identifies host as gpu5, but `nvidia-smi` could not communicate
with the driver and `ps -u emanuele` could not resolve that username inside this
environment. These are visibility limits, not proof of a machine-wide driver
failure or no jobs. Check actual execution hosts through the configured access:
`hostname`, `id`, `nvidia-smi`, `ps -eo uid,pid,ppid,lstart,args` filtered to known
job IDs, plus external `jobs/` and claim records. Never kill or steal claims from
an unidentified process. Verify resource availability again immediately at launch.
Use available idle hosts; no fixed GPU 1 reservation carries forward.

Pinned Python: `/home/itec/emanuele/.conda/envs/pointstream/bin/python`.
`python` is absent in the coordinator default shell; use the pinned interpreter
or `python3` for dependency-free metadata checks. Import sqlite3 before torch,
set worktree PYTHONPATH, and keep caches local per docs/setup.md. The home is NFS:
batch independent reads; avoid expensive serial scans. gh network operations need
sandbox escalation here; routine authorized git/PR work has been approved.

## Immediate next steps and acceptance decisions

1. Read this handoff, PLAN and campaign; fetch current main/PRs and check clean
   worker heads. Keep the latest CI evidence tied to exact heads above. Do not
   start by rereading every old report or rerunning the whole test/codec matrix.
2. Review #104 and #108 independently (parallel bounded subagents are authorized).
   For #104: reproduce prior malformed-row cases, verify exact file/pin hashes,
   map actual producer outputs, confirm claim-specific exclusions and RD-without-
   timing behavior. For #108: verify no lease-only takeover, actual child/group
   exit before release, token ownership, unavailable query rejection, CPU scope
   and monitor integration. Inspect actual code to resolve contradictory reports.
3. Review #109's actual trainer path and named evidence: real CLI/fresh-process
   continuation includes sampler/optimizer/RNG and next update, missing controls
   actually run, selection uses producer field names and preserves tradeoffs,
   explicit residual-off stays off (latest commit restores unset defaults).
   Verify integration through Cursor's accepted contract on the combined revision.
4. Merge independently acceptable PRs with required checks; resolve conflicts
   without restoring obsolete status/adapter files. A remaining generator issue
   must not block accepted generation-free anchor/background work. Return only
   concrete residual failures; fix small authorized integration issues directly
   where efficient rather than reflexively dispatching another full repair wave.
5. Once #104/#108 are accepted, release Cursor's costed 48-frame low-resolution
   anchor card and Antigravity E04 removal-off/background preparation/probe in
   parallel, under actual per-host resource allocation. Neural anchor setup can
   run separately. Release E05's first bounded training stage only after #109's
   real readiness evidence passes; E04 is needed for final whole-codec selection,
   not every low-resolution learning sanity step.
6. Complete score-free confirmation scene/overlap eligibility prospectively;
   keep held-out scores untouched until codec selection procedure is frozen.
   Review Sep 20 freeze feasibility against actual remaining measurements; keep
   Sep 30 submission fixed unless user decides otherwise. Setup/paper structure
   can advance early, result claims wait for evidence.

The user expects workers to report back to this coordinating task (now the new
session) for subsequent tasks. Give copy-ready prompts after acceptance and keep
shared coordination docs on main. If a scientific or resource roadblock remains,
state evidence, attempted remedies/cost and two bounded alternatives needing the
user's insight. Do not silently weaken the neural-winner/secondary-domain goals
or convert exploratory diagnostics into submission claims.
