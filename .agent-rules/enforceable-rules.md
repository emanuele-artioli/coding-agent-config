# Enforceable rules register

Maps host “must” rules (prose in `AGENTS.md` / harness) to a real mechanism.
Prose alone is not enforcement — agents forget rules that are already in
context. Soft first; harden only after the advisory path proves useful.

| Rule id | Prose source | Mechanism | Mode | Notes |
|---|---|---|---|---|
| `plan-waves` | `AGENTS.md` — Plan mode: parallel-agent waves | `scripts/lint_plan_waves.py` (+ Cursor `stop` advisory) | **soft** | Pass = waves heading *or* explicit `skipped: sequential/small`. Hard fail later (Cursor `.cursor/plans/`). |
| `soft-task-change` | progressive nudges | `scripts/context_nudge.py` via `beforeSubmitPrompt` | **log-only** | Do not enable `CONTEXT_NUDGE_BLOCK_SOFT=1` until soft plan-wave nudges are proven. |
| `architecture-freshness` | living diagrams | `scripts/render_architecture.py --check` | **standalone gate** | Not folded into `install.py --check`. |
| `model-family` | `harness/{cursor,claude,antigravity}.md` — Model family | `scripts/guardlib/model_family.py` + Cursor `scripts/cursor/before-task.py` (`preToolUse`/`Task`, `subagentStart`) + Claude `PreToolUse`/`Agent\|Task` → `scripts/guard-model-family.py` | **hard** (family) on Cursor Task path and Claude `PreToolUse`; soft on Antigravity until pending-verification closes. **soft** (rung nudge, added 2026-07-28) everywhere | Prefer omit/inherit; family prefixes only (no versioned slugs). Distrust pstack multi-family defaults. Claude wired 2026-07-28 (script-level verified; the `Agent` tool's own schema already blocks off-family models before the hook runs, so live hook-deny proof is N/A-by-design — see `candidates/pending-verification/claude.md`). Antigravity adapter shipped in SoT. Effort-tier data: `effort-models.json` maps junior/senior/escalation → model per platform for **subagent spawns only** (never the user's own session model); `model_family.tier_nudge()` fires only once the family check already passed. Claude surfaces it as `permissionDecision: "ask"`. Cursor cannot — live 2026-07-28 confirmed `{"permission":"ask"}` on `preToolUse`/`subagentStart` is rejected ("not yet implemented"); stays allow + log (+ `agent_message`). Antigravity is exit-code only (0/2), log-only-by-design for the soft tier. Dated 2026-09-18: API Pareto ≠ subscription ranking; volume children + `STUCK` escape live in README / `session` / `stuck-escalation` (prose + skill, not a hook). |
| `role-contract` | `session` skill — dispatch contract and routing table | `scripts/verify_roles.py` | **standalone gate** | Run after editing `skills/` or `agents/`. Checks every routed role has an agent file and every agent file carries the contract fields. |
| `report-contract` | `session` skill — report contract | `scripts/guardlib/report_contract.py` + Claude `SubagentStop` → `scripts/claude/subagent-stop.py`, Cursor `subagentStop` → `scripts/cursor/subagent-stop.py` | **soft**, advisory system message | Claude wiring is the senior's step this wave; Cursor adapter authored, not wired — `candidates/pending-verification/cursor.md`. |
| `paper-markers` | `update-paper` and `paper-outline` skills | `scripts/paper-markers-lint.py <paper-dir>` | **standalone gate** | Exit 1 on a `CLAIM` without an existing `src=` path; also flags a `HOLE` surviving its `CLAIM` and duplicate ids. |
| `lesson-promotion` | `AGENTS.md` — register of things that have gone wrong more than once | `scripts/closeout.py` receipt + `scripts/lessons.py` (`match` / `record`) called from `end-of-session` step 2; `promote-*.md` counted by `scripts/candidate-reminders.py` at SessionStart | **soft** | The closeout tool deduplicates a boundary/event and never chooses the lesson or delivery. Twice is a rule, but only if the second time finds the first. `match` is token overlap over frontmatter summaries + slugs — a restatement in different words scores zero, so the skill prints the full list under 60 entries and the model decides. Promotion itself is `evaluate-candidates`' `recurring` branch (judgement: which rung, and escalate one rung when the previous status was `applied`). Frontmatter-only reads; the SessionStart count is `readdir` by filename prefix, no `open()` per file. |
| `worktree-cleanup` | `AGENTS.md` — Plan mode: parallel-agent waves | `scripts/guardlib/worktree_cleanup.py` + `scripts/clean-merged-worktrees.py` via host `post-merge` git hook (`core.hooksPath`) | **automated** | Failsafe: only removes clean worktrees whose commits are 100% merged into `origin/main` (or `origin/master`). Refuses to touch dirty or unmerged worktrees. Chained to local repo hooks. |

## Adding a rule

1. Put the must in `AGENTS.md` (or harness) if it isn’t there.
2. Add a row here with a concrete mechanism (script, hook, CI gate).
3. Prefer soft advisory until the fix message is clear and false positives are rare.
4. Platform live wiring for non-Cursor agents → `candidates/pending-verification/<platform>.md`.
